query GetMyAppointments($status: String) {
  appointments(status: $status) {
    id
    startTime
    endTime
    encounterMode
    cost
    paymentCompleted
    rastucId
    status
    statusDisplay
    doctor {
      id
      fullName
      title
      primarySpecialty {
        name
      }
    }
  }
}
