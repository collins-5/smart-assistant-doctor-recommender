mutation BookAppointment($bookingArgs: AppointmentInput!) {
  bookAppointment(bookingArgs: $bookingArgs) {
    appointment {
      id
      rastucId
      startTime
      encounterMode
      cost
      doctor {
        id
        fullName
        title
        primarySpecialty {
          name
        }
      }
    }
  }
}
